<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/hotels">TripNest</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navMenu">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item"><a class="nav-link" href="/hotels">Hotels</a></li>
        <% if (currentUser) { %>
          <li class="nav-item"><a class="nav-link" href="/bookings/dashboard">My Bookings</a></li>
        <% } %>
        <% if (currentUser && currentUser.role === 'admin') { %>
          <li class="nav-item"><a class="nav-link" href="/admin">Admin Panel</a></li>
        <% } %>
      </ul>
      <div class="d-flex align-items-center gap-2">
        <% if (currentUser) { %>
          <span class="text-white small">Hi, <%= currentUser.name %></span>
          <form action="/logout" method="POST" class="m-0">
            <button class="btn btn-outline-light btn-sm" type="submit">Logout</button>
          </form>
        <% } else { %>
          <a class="btn btn-light btn-sm" href="/login">Login</a>
          <a class="btn btn-warning btn-sm" href="/signup">Signup</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
